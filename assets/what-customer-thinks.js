$('.wct-carousel').flickity({
    
    cellAlign: 'left',
    contain: true,
    freeScroll: true,
    groupCells: true,

    // disable previous & next buttons and dots
    prevNextButtons: true,
    pageDots: true,
  });
